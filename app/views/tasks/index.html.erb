<h1>ALL THE TASKS</h1>

<ul>
  <% @tasks.each do |t| %>
    <li><%= link_to t.title, task_path(t) %><%= link_to "🗑", task_path(t),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></li>
  <% end %>
</ul>

<%= link_to "Create a task", new_task_path(@task)%>